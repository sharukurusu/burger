<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}Devour it!{{else}}Cook again!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Toss it to the dog!</button>
</li>
