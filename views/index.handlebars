<h1>Burgers! Get Em' While They're <span style="color: red;">Hot!!</span></h1>

<form class="create-form">
    <h1>Add a burger</h1>
    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="name">
    </div>

  {{!-- <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="0" checked> devoured!<br>
    <input type="radio" name="devoured" value="1"> Not eaten
  </div> --}}

  <button type="submit">Add burger</button>
</form>
<h2>Burgers ready to eat!</h2>

<ul class="cooked">
    <li id="topBun"></li>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=1}}
    {{/unless}}
  {{/each}}
  <li id="bottomBun"></li>
</ul>

<h2>Burgers you can cook!</h2>

<ul class="raw">
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=0}}
    {{/if}}
  {{/each}}
</ul>


